//! Native bridge to the `codebase-memory-mcp` CLI.
//!
//! See `core::codebase::commands` for the Tauri command surface and
//! `core::codebase::helpers` for the spawn / parse implementation.

pub mod commands;
pub mod helpers;
pub mod models;
